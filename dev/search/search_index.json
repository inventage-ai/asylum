{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Asylum","text":"<p>Docker sandbox for AI coding agents. Single binary, runs on Linux and macOS (ARM and x86).</p> <p>Asylum wraps Docker to give Claude Code, Gemini CLI, and Codex a full development environment with Python, Node.js, Java, and Docker-in-Docker \u2014 while keeping your host clean. Containers are ephemeral, but caches, auth, and history persist.</p>"},{"location":"#install","title":"Install","text":"<pre><code>curl -fsSL https://raw.githubusercontent.com/inventage-ai/asylum/main/install.sh | sh\n</code></pre> <p>Or download a binary from the releases page and put it in your PATH.</p> <p>Requires: Docker installed and running.</p>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code>cd your-project/\n\n# Start Claude Code (default)\nasylum\n\n# Start Gemini CLI\nasylum -a gemini\n\n# Start Codex\nasylum -a codex\n\n# Interactive shell (no agent)\nasylum shell\n</code></pre> <p>On first run, Asylum builds a Docker image (~5 min) and seeds agent config from your host. Subsequent runs start in seconds.</p> <p>See Getting Started for a full walkthrough of your first run.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Docker installed and running</li> <li>An AI coding agent installed on your host (Claude Code, Gemini CLI, or Codex) \u2014 Asylum seeds its config from your host installation</li> </ul>"},{"location":"getting-started/#install-asylum","title":"Install Asylum","text":"<pre><code>curl -fsSL https://raw.githubusercontent.com/inventage-ai/asylum/main/install.sh | sh\n</code></pre> <p>This detects your OS and architecture and downloads the correct binary.</p>"},{"location":"getting-started/#first-run","title":"First Run","text":"<pre><code>cd your-project/\nasylum\n</code></pre> <p>On the very first run, Asylum:</p> <ol> <li>Builds the base image (<code>asylum:latest</code>) \u2014 a full dev toolchain with all three agent CLIs, language runtimes, and tools. This takes ~5 minutes and is cached for all projects.</li> <li>Seeds agent config \u2014 copies your host agent configuration (e.g., <code>~/.claude</code>) to <code>~/.asylum/agents/claude/</code>. This is a one-time copy; the asylum copy is independent after seeding.</li> <li>Starts a container \u2014 mounts your project directory, caches, and agent config, then launches the agent.</li> </ol> <p>Subsequent runs start in seconds since the image is cached.</p>"},{"location":"getting-started/#what-happens-inside","title":"What Happens Inside","text":"<p>When the container starts, you get:</p> <ul> <li>Your project mounted at its real host path (not <code>/workspace</code>) \u2014 absolute paths and git worktrees work correctly</li> <li>Agent config, SSH keys, git config, and shell history mounted from <code>~/.asylum/</code></li> <li>Package caches (npm, pip, maven, gradle) persisted in named Docker volumes</li> <li>A full dev environment: Python 3 + uv, Node.js LTS + fnm, Java 17/21/25 + mise, build tools, and more</li> </ul>"},{"location":"getting-started/#choosing-an-agent","title":"Choosing an Agent","text":"<pre><code>asylum              # Claude Code (default)\nasylum -a gemini    # Gemini CLI\nasylum -a codex     # Codex\n</code></pre> <p>Each agent runs in YOLO mode by default (auto-approve all actions). Agent config is stored separately per agent in <code>~/.asylum/agents/&lt;agent&gt;/</code>.</p>"},{"location":"getting-started/#project-configuration","title":"Project Configuration","text":"<p>Create a <code>.asylum</code> file in your project root to customize the environment:</p> <pre><code>agent: gemini\n\nkits:\n  java:\n    versions: [\"17\"]\n  node:\n    packages:\n      - \"@anthropic-ai/claude-mcp-server-filesystem\"\n\nports:\n  - \"3000\"\n  - \"8080:80\"\n\nenv:\n  DEBUG: \"true\"\n</code></pre> <p>See Configuration for all options, and Kits for available language and tool kits.</p>"},{"location":"getting-started/#ssh","title":"SSH","text":"<p>SSH keys are managed automatically by the always-on SSH kit. On first container start, an Ed25519 key pair is generated at <code>~/.asylum/ssh/</code> and mounted into <code>~/.ssh/</code>. Add the printed public key to GitHub/GitLab.</p> <p>You can configure SSH isolation in your config:</p> <pre><code>kits:\n  ssh:\n    isolation: isolated   # default \u2014 generated keys in ~/.asylum/ssh/\n    # isolation: shared   # mount host ~/.ssh/ directly\n    # isolation: project  # per-project keys\n</code></pre>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<ul> <li>Commands \u2014 all available commands and flags</li> <li>Configuration \u2014 layered YAML config system</li> <li>Kits \u2014 language and tool kits</li> <li>Concepts \u2014 how images, mounts, and sessions work</li> </ul>"},{"location":"commands/","title":"Commands","text":""},{"location":"commands/#usage","title":"Usage","text":"<pre><code>asylum [flags]                Start default agent\nasylum [flags] -- [args]      Start agent with extra args\nasylum [flags] shell          Interactive zsh shell\nasylum [flags] shell --admin  Admin shell with sudo notice\nasylum [flags] run &lt;cmd&gt;      Run command in container\nasylum cleanup                Remove current project's container, volumes, and data\nasylum cleanup --all          Remove all Asylum images, volumes, and cached data\nasylum version [--short]      Show version\nasylum self-update [version]  Update to latest (or specific) version\nasylum self-update --dev      Update to latest dev build\nasylum self-update --safe     Emergency update (always dev, no checks)\n</code></pre>"},{"location":"commands/#default-agent-mode","title":"Default (Agent Mode)","text":"<p>Running <code>asylum</code> with no subcommand starts the configured agent (default: Claude Code) in the current project directory. The agent runs in YOLO mode (auto-approve all actions).</p> <p>Use <code>--</code> to pass extra flags to the agent:</p> <pre><code>asylum -- --verbose\nasylum -a gemini -- --sandboxed\n</code></pre> <p>See CLI Flags for all available flags.</p>"},{"location":"commands/#subcommands","title":"Subcommands","text":"Command Description <code>shell</code> Interactive zsh shell in the container <code>run</code> Run a command in the container <code>cleanup</code> Clean up current project (or <code>--all</code> for everything) <code>version</code> Show version information <code>self-update</code> Update Asylum to a new version"},{"location":"commands/cleanup/","title":"cleanup","text":"<p>Remove Asylum containers, volumes, and cached data.</p>"},{"location":"commands/cleanup/#usage","title":"Usage","text":"<pre><code>asylum cleanup          # current project only\nasylum cleanup --all    # everything\nasylum --cleanup        # flag alias (current project)\n</code></pre>"},{"location":"commands/cleanup/#description","title":"Description","text":"<p>By default, <code>cleanup</code> scopes to the current project: it removes the project's container, its Docker volumes, and its project data directory. Other projects and the base image are untouched.</p> <p>Use <code>--all</code> for a global cleanup that removes all Asylum images, volumes, and optionally host-side cached data.</p> <p>Agent configuration (<code>~/.asylum/agents/</code>) is always preserved since it contains auth tokens and session data.</p>"},{"location":"commands/cleanup/#project-cleanup-default","title":"Project Cleanup (default)","text":"<p>Running <code>asylum cleanup</code> from a project directory removes:</p> Resource Removed Project container Yes Project volumes (<code>&lt;container&gt;-*</code>) Yes Project data (<code>~/.asylum/projects/&lt;container&gt;/</code>) Yes Port allocation for project Yes Base image No Other projects No <p>If run outside a project directory, Asylum suggests using <code>--all</code> instead.</p>"},{"location":"commands/cleanup/#global-cleanup-all","title":"Global Cleanup (<code>--all</code>)","text":"<p>Running <code>asylum cleanup --all</code> shows all resources that will be removed and asks for confirmation before proceeding:</p> <pre><code>The following resources will be removed:\n\n  Images:\n    asylum:latest\n    asylum:proj-abc123def456\n\n  Volumes:\n    asylum-7a3f2b-myapp-npm\n    asylum-7a3f2b-myapp-pip\n\nProceed? (y/N)\n</code></pre> <p>After removing images and volumes, a second prompt offers to remove host-side cached data:</p> <pre><code>Remove cached data (~/.asylum/cache/ and ~/.asylum/projects/)? (y/N)\n</code></pre> <p>Global cleanup requires an interactive terminal \u2014 it won't run in scripts or CI.</p>"},{"location":"commands/run/","title":"run","text":"<p>Run a command inside the container.</p>"},{"location":"commands/run/#usage","title":"Usage","text":"<pre><code>asylum run &lt;command&gt; [args...]\nasylum run -- &lt;command&gt; [args...]\n</code></pre>"},{"location":"commands/run/#description","title":"Description","text":"<p>Executes a one-off command in the container. If no container is running, one is started first. The <code>--</code> separator is optional.</p>"},{"location":"commands/run/#examples","title":"Examples","text":"<pre><code># Run a test suite\nasylum run python test.py\n\n# List files\nasylum run ls -la\n\n# Run with explicit separator\nasylum run -- npm test\n\n# Combine with flags\nasylum -p 8080 run python -m http.server\n</code></pre>"},{"location":"commands/run/#notes","title":"Notes","text":"<ul> <li>The command runs as your host user (same username, UID, and home directory).</li> <li>The exit code from the command is forwarded to the host.</li> <li>The container persists after the command finishes if other sessions are still attached. If this was the only session, the container is removed.</li> </ul>"},{"location":"commands/self-update/","title":"self-update","text":"<p>Update Asylum to a new version.</p>"},{"location":"commands/self-update/#usage","title":"Usage","text":"<pre><code>asylum self-update              # latest stable release\nasylum self-update 0.5.0        # specific version\nasylum self-update --dev         # latest dev build from main\nasylum self-update --safe        # emergency update (always dev, no checks)\nasylum selfupdate               # alias\n</code></pre>"},{"location":"commands/self-update/#description","title":"Description","text":"<p>Downloads and replaces the Asylum binary with a new version. The update is atomic \u2014 the new binary is downloaded to a temp file and renamed into place.</p>"},{"location":"commands/self-update/#flags","title":"Flags","text":"Flag Description <code>--dev</code> Update to the latest dev build (rolling release from <code>main</code>) <code>--safe</code> Emergency update: always pulls dev, skips all version checks"},{"location":"commands/self-update/#channels","title":"Channels","text":"<p>Asylum supports two release channels:</p> Channel Source Use case <code>stable</code> Latest GitHub release Default, recommended for most users <code>dev</code> Rolling <code>dev</code> pre-release from <code>main</code> Latest features, may be unstable <p>To always track dev builds, set the release channel in your global config:</p> <pre><code># ~/.asylum/config.yaml\nrelease-channel: dev\n</code></pre> <p>With this setting, <code>asylum self-update</code> (without <code>--dev</code>) will pull from the dev channel.</p>"},{"location":"commands/self-update/#examples","title":"Examples","text":"<pre><code># Update to latest stable\nasylum self-update\n\n# Install a specific version\nasylum self-update 0.4.0\n\n# Track dev builds\nasylum self-update --dev\n\n# Emergency: binary is broken, just get something working\nasylum self-update --safe\n</code></pre>"},{"location":"commands/self-update/#notes","title":"Notes","text":"<ul> <li>On the stable channel, <code>self-update</code> skips the download if you're already on the latest version.</li> <li>On the dev channel, it shows a changelog of recent commits when the build changes.</li> <li>If the binary is in a system directory, you may need to run with <code>sudo</code>.</li> </ul>"},{"location":"commands/shell/","title":"shell","text":"<p>Start an interactive zsh shell inside the container.</p>"},{"location":"commands/shell/#usage","title":"Usage","text":"<pre><code>asylum shell\nasylum shell --admin\n</code></pre>"},{"location":"commands/shell/#description","title":"Description","text":"<p>Opens a zsh shell in the running container (or starts a new container first). Useful for manual debugging, running tests, or exploring the container environment.</p> <p>The <code>--admin</code> flag opens a root shell with a notice that you're running as root. This is needed for installing system packages or modifying files owned by root.</p>"},{"location":"commands/shell/#flags","title":"Flags","text":"Flag Description <code>--admin</code> Open a root shell instead of the default user shell"},{"location":"commands/shell/#examples","title":"Examples","text":"<pre><code># Open a regular shell\nasylum shell\n\n# Open a root shell to install a system package\nasylum shell --admin\napt-get install -y some-package\n\n# Shell with port forwarding\nasylum -p 3000 shell\n</code></pre>"},{"location":"commands/shell/#notes","title":"Notes","text":"<ul> <li>Multiple shell sessions can share the same container. The container is removed when the last session exits.</li> <li>Global flags like <code>-p</code> and <code>-v</code> are applied when the container is first created, not when exec-ing into an existing one.</li> </ul>"},{"location":"commands/version/","title":"version","text":"<p>Show Asylum version information.</p>"},{"location":"commands/version/#usage","title":"Usage","text":"<pre><code>asylum version\nasylum version --short\nasylum --version          # flag alias\nasylum --version --short  # flag alias\n</code></pre>"},{"location":"commands/version/#description","title":"Description","text":"<p>Prints the Asylum version string and exits.</p>"},{"location":"commands/version/#flags","title":"Flags","text":"Flag Description <code>--short</code> Print only the version number without the <code>asylum</code> prefix"},{"location":"commands/version/#examples","title":"Examples","text":"<pre><code>$ asylum version\nasylum 0.5.0 (abc1234)\n\n$ asylum version --short\n0.5.0 (abc1234)\n</code></pre>"},{"location":"concepts/","title":"Concepts","text":"<p>How Asylum works under the hood.</p> <ul> <li>Images \u2014 two-tier image strategy and rebuild detection</li> <li>Mounts \u2014 what's mounted into containers and why</li> <li>Sessions \u2014 multi-session support and agent resume</li> <li>Agents \u2014 supported agents, config seeding, and resume behavior</li> <li>Config Isolation \u2014 shared, isolated, and project isolation modes</li> </ul>"},{"location":"concepts/agents/","title":"Agents","text":"<p>Asylum supports three AI coding agents. Each runs in YOLO mode (auto-approve all actions) by default.</p>"},{"location":"concepts/agents/#supported-agents","title":"Supported Agents","text":"Agent Binary Default Mode Config Dir Claude Code <code>claude</code> <code>--dangerously-skip-permissions</code> <code>~/.claude</code> Gemini CLI <code>gemini</code> <code>--yolo</code> <code>~/.gemini</code> Codex <code>codex</code> <code>--yolo</code> <code>~/.codex</code>"},{"location":"concepts/agents/#selecting-an-agent","title":"Selecting an Agent","text":"<pre><code>asylum                # Claude Code (default)\nasylum -a gemini      # Gemini CLI\nasylum -a codex       # Codex\n</code></pre> <p>Or set it in your config:</p> <pre><code>agent: gemini\n</code></pre>"},{"location":"concepts/agents/#config-isolation","title":"Config Isolation","text":"<p>Asylum controls how each agent's config directory is managed inside the container. Three modes are available: <code>shared</code> (host config mounted directly), <code>isolated</code> (default \u2014 separate copy in <code>~/.asylum/agents/&lt;agent&gt;/</code>), and <code>project</code> (per-project copy).</p> <p>On first run with Claude, Asylum prompts you to choose a mode. See Config Isolation for full details.</p>"},{"location":"concepts/agents/#passing-extra-args","title":"Passing Extra Args","text":"<p>Use <code>--</code> to pass flags to the agent:</p> <pre><code>asylum -- --verbose\nasylum -a gemini -- --sandboxed\n</code></pre>"},{"location":"concepts/agents/#resume-behavior","title":"Resume Behavior","text":"<p>Each agent resumes its previous session by default. Use <code>-n</code> to start fresh. See Sessions for details on how each agent detects previous sessions.</p>"},{"location":"concepts/agents/#installing-multiple-agents","title":"Installing Multiple Agents","text":"<p>By default, only Claude Code is installed in the base image. To install additional agents:</p> <pre><code>agents:\n  claude: {}\n  gemini: {}\n  codex: {}\n</code></pre> <p>Or via CLI:</p> <pre><code>asylum --agents claude,gemini\n</code></pre> <p>Agent installation requires their kit dependencies (Gemini and Codex need the <code>node</code> kit).</p>"},{"location":"concepts/images/","title":"Images","text":"<p>Asylum uses a two-tier image strategy to balance build time with per-project customization.</p>"},{"location":"concepts/images/#base-image","title":"Base Image","text":"<p>Tag: <code>asylum:latest</code></p> <p>The base image contains the full dev toolchain shared across all projects:</p> <ul> <li>OS: Debian Trixie</li> <li>Languages: Python 3 + uv, Node.js LTS + fnm, JDK 17/21/25 + mise</li> <li>Build tools: gcc, g++, make, cmake</li> <li>Shell: zsh, bash, oh-my-zsh</li> <li>Dev tools: git, vim, nano, tmux, htop, ripgrep, fd, jq, yq, direnv</li> <li>Network: curl, wget, openssh-client, netcat</li> <li>Docker CLI (not the engine \u2014 see the Docker kit for Docker-in-Docker)</li> <li>Agent CLIs: Claude Code, Gemini CLI, Codex</li> </ul> <p>The base image is built once (~5 minutes) and cached. It rebuilds automatically when the Dockerfile or kit snippets change (hash-based detection).</p>"},{"location":"concepts/images/#project-image","title":"Project Image","text":"<p>Tag: <code>asylum:proj-&lt;hash&gt;</code></p> <p>When your config includes packages (apt, npm, pip, or custom build commands), Asylum builds a project image on top of the base:</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  Project Image               \u2502\n\u2502  (apt packages, npm globals, \u2502\n\u2502   pip tools, custom builds)  \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  Base Image                  \u2502\n\u2502  (languages, agents, tools)  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>If no packages are configured, the base image is used directly \u2014 no project image is built.</p> <p>Project images are also cached and only rebuild when the package configuration changes.</p>"},{"location":"concepts/images/#rebuild-detection","title":"Rebuild Detection","text":"<p>Both tiers use hash-based detection:</p> <ul> <li>Base image: SHA-256 of the Dockerfile, kit snippets, agent install snippets, and host user identity (username, UID, GID, home directory). Stored as a Docker label (<code>asylum.hash</code>).</li> <li>Project image: SHA-256 of the packages config. Stored as a Docker label (<code>asylum.packages.hash</code>).</li> </ul> <p>A base image rebuild invalidates all project images (they're built <code>FROM asylum:latest</code>).</p>"},{"location":"concepts/images/#forcing-a-rebuild","title":"Forcing a Rebuild","text":"<pre><code>asylum --rebuild\n</code></pre> <p>This forces both tiers to rebuild, ignoring the cached hashes.</p>"},{"location":"concepts/images/#cleanup","title":"Cleanup","text":"<p>Remove all Asylum images with:</p> <pre><code>asylum cleanup\n</code></pre> <p>See <code>cleanup</code> for details.</p>"},{"location":"concepts/isolation/","title":"Config Isolation","text":"<p>Asylum controls how each agent's config directory (e.g. <code>~/.claude</code>) is managed inside the container. Three isolation modes determine where config lives on the host and how it's mounted.</p>"},{"location":"concepts/isolation/#modes","title":"Modes","text":""},{"location":"concepts/isolation/#shared","title":"<code>shared</code>","text":"<p>The host config directory is mounted directly into the container. Changes sync both ways \u2014 anything the agent writes inside the container is immediately visible on the host, and vice versa.</p> Path Host <code>~/.claude</code> Container <code>~/.claude</code> <p>Use when: you want identical config inside and outside the sandbox, or use the same API keys, MCP servers, and settings everywhere.</p> <p>Trade-off: no isolation. A misbehaving agent can modify your host config.</p>"},{"location":"concepts/isolation/#isolated-default","title":"<code>isolated</code> (default)","text":"<p>Agent config is stored in Asylum's own directory, separate from the host. On first run, Asylum seeds this by copying from the host config. After that, the two are independent.</p> Path Host <code>~/.asylum/agents/claude/</code> Container <code>~/.claude</code> <p>Use when: you want a stable sandbox config that doesn't drift with host changes, but is shared across all projects.</p> <p>Trade-off: changes to your host config (e.g. new MCP servers) won't appear in the container. You need to update the asylum copy separately, or delete it to re-trigger seeding.</p>"},{"location":"concepts/isolation/#project","title":"<code>project</code>","text":"<p>Each project gets its own independent agent config directory. On first run, Asylum seeds it from the host config, just like <code>isolated</code> mode.</p> Path Host <code>~/.asylum/projects/&lt;container&gt;/claude-config/</code> Container <code>~/.claude</code> <p>Use when: you need completely separate agent state per project \u2014 different settings, different conversation history, no cross-contamination.</p> <p>Trade-off: more directories to manage. Config changes must be made per-project.</p>"},{"location":"concepts/isolation/#configuration","title":"Configuration","text":"<p>Set the isolation mode in your config:</p> <pre><code># ~/.asylum/config.yaml (or .asylum / .asylum.local)\nagents:\n  claude:\n    config: isolated   # shared | isolated | project\n</code></pre>"},{"location":"concepts/isolation/#first-run-wizard","title":"First-Run Wizard","text":"<p>On first run with Claude, Asylum prompts you to choose an isolation mode if one isn't configured. The choice is saved to <code>~/.asylum/config.yaml</code>. In non-interactive environments, <code>isolated</code> is used as the default.</p> <p>Other agents (Gemini, Codex) currently default to <code>isolated</code> without prompting.</p>"},{"location":"concepts/isolation/#config-seeding","title":"Config Seeding","text":"<p>In <code>isolated</code> and <code>project</code> modes, Asylum performs a one-time copy of your host agent config into the target directory on first run. This seeds API keys, settings, and other config so you don't have to set them up again.</p> <p>After seeding, the copy is independent \u2014 changes to the host config don't propagate. To re-seed, delete the asylum copy and restart.</p> <p>Seeding is skipped in <code>shared</code> mode since the host directory is used directly.</p>"},{"location":"concepts/isolation/#first-run-session-behavior","title":"First-Run Session Behavior","text":"<p>When config is freshly seeded, Asylum forces a new agent session (<code>-n</code> behavior) regardless of what the seeded data contains. The seeded files may include session state from the host, but they don't represent a container session.</p>"},{"location":"concepts/mounts/","title":"Mounts","text":"<p>Asylum mounts your project, config, caches, and tools into the container. Your project is mounted at its real host path \u2014 not <code>/workspace</code> \u2014 so absolute paths and git worktrees work correctly.</p>"},{"location":"concepts/mounts/#default-mounts","title":"Default Mounts","text":"What Host Path Container Path Mode Project directory <code>$PWD</code> <code>$PWD</code> Read-write Git config <code>~/.gitconfig</code> <code>/tmp/host_gitconfig</code> Read-only SSH keys Depends on SSH isolation <code>~/.ssh/</code> Read-only (keys), Read-write (known_hosts) Agent config Depends on isolation mode Agent-specific path Read-write Shell history <code>~/.asylum/projects/&lt;id&gt;/history/</code> <code>~/.shell_history/</code> Read-write Direnv approvals <code>~/.local/share/direnv/allow</code> Same path Read-only <code>.env</code> file <code>$PWD/.env</code> Loaded as <code>--env-file</code> \u2014"},{"location":"concepts/mounts/#cache-volumes","title":"Cache Volumes","text":"<p>Package caches are stored in named Docker volumes (not host bind mounts), scoped per project:</p> Cache Container Path Kit npm <code>~/.npm</code> node/npm pip <code>~/.cache/pip</code> python/uv Maven <code>~/.m2</code> java/maven Gradle <code>~/.gradle</code> java/gradle <p>Named volumes persist across container restarts and are shared between sessions on the same project.</p>"},{"location":"concepts/mounts/#shadow-node_modules","title":"Shadow node_modules","text":"<p>When the Node.js kit is active (with <code>shadow-node-modules: true</code>, the default), Asylum creates a named Docker volume for each <code>node_modules</code> directory found in the project. This isolates Linux-built native binaries from macOS host binaries.</p> <p>See Node.js Kit \u2014 Shadow node_modules for details.</p>"},{"location":"concepts/mounts/#git-worktrees","title":"Git Worktrees","text":"<p>If the project directory is a git worktree, Asylum detects and mounts both the worktree's <code>.git</code> file and the main repository's <code>.git</code> directory. This ensures git operations work correctly inside the container.</p>"},{"location":"concepts/mounts/#custom-volumes","title":"Custom Volumes","text":"<p>Add extra volumes via config or CLI:</p> <pre><code># .asylum\nvolumes:\n  - ~/shared-data:/data:ro\n  - /tmp/exchange:/tmp/exchange\n</code></pre> <pre><code>asylum -v ~/data:/data:ro\n</code></pre> <p>Tilde (<code>~</code>) is expanded. Supported options: <code>ro</code>, <code>rw</code>, <code>z</code>, <code>Z</code>, <code>shared</code>, <code>cached</code>, <code>delegated</code>, and others.</p>"},{"location":"concepts/mounts/#host-ip-access","title":"Host IP Access","text":"<p>The host machine is accessible from inside the container via <code>host.docker.internal</code>.</p>"},{"location":"concepts/sessions/","title":"Sessions","text":"<p>Asylum supports multiple concurrent sessions sharing a single container, with automatic session resume for agents.</p>"},{"location":"concepts/sessions/#multi-session","title":"Multi-Session","text":"<p>Multiple terminals can share the same container:</p> <ol> <li>The first <code>asylum</code> invocation starts the container</li> <li>Subsequent invocations exec into the running container</li> <li>The container is automatically removed when the last session exits</li> </ol> <p>When a session exits, asylum checks for other active exec sessions inside the container. If none remain, the container is automatically removed.</p>"},{"location":"concepts/sessions/#agent-resume","title":"Agent Resume","text":"<p>Agents automatically resume their previous session by default. This means if you run <code>asylum</code>, close the terminal, and run <code>asylum</code> again, the agent picks up where it left off.</p> <p>Use <code>-n</code> / <code>--new</code> to start a fresh session:</p> <pre><code>asylum -n\n</code></pre>"},{"location":"concepts/sessions/#how-resume-detection-works","title":"How Resume Detection Works","text":"<p>Each agent has its own method for detecting previous sessions:</p> <ul> <li>Claude Code: Looks for <code>.jsonl</code> session files in <code>~/.asylum/agents/claude/projects/&lt;encoded-path&gt;/</code></li> <li>Gemini CLI: Checks for a <code>.project_root</code> file and chat history in <code>~/.asylum/agents/gemini/tmp/</code></li> <li>Codex: Uses a marker file at <code>~/.asylum/agents/codex/projects/&lt;encoded-path&gt;/.has_session</code></li> </ul>"},{"location":"concepts/sessions/#first-run-exception","title":"First Run Exception","text":"<p>On the very first run (when agent config is seeded from the host), resume is skipped. The seeded data doesn't represent a container session, so the agent starts fresh.</p>"},{"location":"concepts/sessions/#container-naming","title":"Container Naming","text":"<p>Container names are deterministic based on the project directory:</p> <pre><code>asylum-&lt;sha256(project_dir)[:6]&gt;\n</code></pre> <p>This means the same project directory always gets the same container name, enabling session persistence and resume detection.</p>"},{"location":"concepts/sessions/#container-hostname","title":"Container Hostname","text":"<p>The container hostname is derived from the project directory basename:</p> <pre><code>asylum-&lt;safe-lowercase-basename&gt;\n</code></pre> <p>Special characters are replaced with hyphens, and the name is capped at 63 characters total.</p>"},{"location":"configuration/","title":"Configuration","text":"<p>Asylum uses a layered YAML config system. Settings are merged from multiple sources, with later sources overriding earlier ones.</p>"},{"location":"configuration/#config-files","title":"Config Files","text":"Priority File Purpose 1 (lowest) <code>~/.asylum/config.yaml</code> Global defaults 2 <code>.asylum</code> Project config (commit this) 3 (highest) <code>.asylum.local</code> Local overrides (gitignore this) <p>CLI flags override all config files.</p>"},{"location":"configuration/#example-asylum","title":"Example <code>.asylum</code>","text":"<pre><code>agent: gemini\n\nkits:\n  java:\n    versions: [\"17\"]\n  node:\n    onboarding: true\n    packages:\n      - \"@anthropic-ai/claude-mcp-server-filesystem\"\n  docker: {}\n  title:\n    tab-title: \"\ud83e\udd16 {project}\"\n\nports:\n  - \"3000\"\n  - \"8080:80\"\n\nvolumes:\n  - ~/shared-data:/data:ro\n\nenv:\n  MY_API_KEY: \"abc123\"\n  DEBUG: \"true\"\n</code></pre>"},{"location":"configuration/#top-level-fields","title":"Top-Level Fields","text":"Field Type Description <code>agent</code> string Default agent: <code>claude</code>, <code>gemini</code>, <code>codex</code> (default: <code>claude</code>) <code>release-channel</code> string Self-update channel: <code>stable</code> or <code>dev</code> <code>kits</code> map Kit configurations (see Kits) <code>agents</code> map Agent configurations (see Config Isolation) <code>ports</code> list Port forwarding rules <code>volumes</code> list Additional volume mounts <code>env</code> map Environment variables"},{"location":"configuration/#kits-configuration","title":"Kits Configuration","text":"<p>Each kit is configured under the <code>kits</code> key. An empty map <code>{}</code> enables the kit with defaults:</p> <pre><code>kits:\n  docker: {}                    # enable with defaults\n  java:\n    versions: [\"17\", \"21\"]      # configure specific options\n    default-version: \"17\"\n  node:\n    shadow-node-modules: false  # disable a feature\n  github:\n    disabled: true              # disable a default-on kit\n</code></pre> <p>See Kits for all available kits and their options.</p>"},{"location":"configuration/#ports","title":"Ports","text":"<p>Forward ports from the container to the host:</p> <pre><code>ports:\n  - \"3000\"        # same port on host and container\n  - \"8080:80\"     # host:container\n</code></pre>"},{"location":"configuration/#volumes","title":"Volumes","text":"<p>Mount additional directories:</p> <pre><code>volumes:\n  - ~/data:/data:ro              # host:container:options\n  - /tmp/shared:/tmp/shared      # absolute paths\n</code></pre> <p>Tilde (<code>~</code>) is expanded to the home directory. Valid mount options: <code>ro</code>, <code>rw</code>, <code>z</code>, <code>Z</code>, <code>shared</code>, <code>slave</code>, <code>private</code>, <code>cached</code>, <code>delegated</code>.</p>"},{"location":"configuration/#environment-variables","title":"Environment Variables","text":"<pre><code>env:\n  API_KEY: \"secret\"\n  DEBUG: \"true\"\n</code></pre> <p>Asylum also reads <code>.env</code> files from the project root (mounted as <code>--env-file</code>).</p>"},{"location":"configuration/#merge-rules","title":"Merge Rules","text":"<p>When multiple config files define the same field:</p> Type Behavior Scalars (agent, release-channel) Last value wins Lists (ports, volumes) Concatenated across layers Maps (env, kits, agents) Merged per key, last value wins"},{"location":"configuration/flags/","title":"CLI Flags","text":"<p>Flags can be used with any subcommand and override all config file settings.</p>"},{"location":"configuration/flags/#general-flags","title":"General Flags","text":"Flag Description <code>-a</code>, <code>--agent &lt;name&gt;</code> Agent to use: <code>claude</code>, <code>gemini</code>, <code>codex</code> (default: <code>claude</code>) <code>-p &lt;port&gt;</code> Forward a port (repeatable, e.g., <code>-p 3000 -p 8080:80</code>) <code>-v &lt;volume&gt;</code> Mount a volume (repeatable, e.g., <code>-v ~/data:/data:ro</code>) <code>-e KEY=VALUE</code> Set environment variable (repeatable, last wins) <code>--java &lt;version&gt;</code> Java version in container <code>--kits &lt;list&gt;</code> Comma-separated kits to enable (e.g., <code>--kits java,python,docker</code>) <code>--agents &lt;list&gt;</code> Comma-separated agents to install (e.g., <code>--agents claude,gemini</code>) <code>-n</code>, <code>--new</code> Start a new session (skip auto-resume) <code>--rebuild</code> Force rebuild the Docker image <code>--skip-onboarding</code> Skip project onboarding tasks for this run <code>--cleanup</code> Alias for <code>cleanup</code> command <code>--version</code> Alias for <code>version</code> command <code>-h</code>, <code>--help</code> Show help"},{"location":"configuration/flags/#agent-passthrough","title":"Agent Passthrough","text":"<p>Use <code>--</code> to pass flags directly to the agent:</p> <pre><code>asylum -- --verbose\nasylum -a gemini -- --sandboxed\n</code></pre> <p>Everything after <code>--</code> is forwarded to the agent command unchanged.</p>"},{"location":"configuration/flags/#examples","title":"Examples","text":"<pre><code># Start Gemini with port forwarding and an env var\nasylum -a gemini -p 3000 -e API_KEY=abc123\n\n# Fresh Claude session with image rebuild\nasylum -n --rebuild\n\n# Use only Java and Docker kits\nasylum --kits java,docker\n</code></pre>"},{"location":"configuration/packages/","title":"Packages","text":"<p>Asylum can install additional packages into a project-specific Docker image layer. When packages are configured, Asylum builds a project image on top of the base image.</p>"},{"location":"configuration/packages/#package-types","title":"Package Types","text":"<p>Packages are configured under each kit's config:</p> <pre><code>kits:\n  apt:\n    packages:\n      - libpq-dev\n      - redis-tools\n  node:\n    packages:\n      - \"@anthropic-ai/claude-mcp-server-filesystem\"\n      - tsx\n  python:\n    packages:\n      - pandas\n      - numpy\n  shell:\n    build:\n      - \"curl -fsSL https://deno.land/install.sh | sh\"\n</code></pre> Kit Key Installer Runs as <code>apt</code> <code>packages</code> <code>apt-get install</code> root <code>node</code> <code>packages</code> <code>npm install -g</code> claude <code>python</code> <code>packages</code> <code>uv tool install</code> claude <code>shell</code> <code>build</code> Executed as-is claude"},{"location":"configuration/packages/#how-it-works","title":"How It Works","text":"<p>When any packages are configured, Asylum:</p> <ol> <li>Generates a project Dockerfile (<code>FROM asylum:latest</code> + install commands)</li> <li>Builds a project image tagged <code>asylum:proj-&lt;hash&gt;</code></li> <li>Caches the image \u2014 rebuilds only when packages change</li> </ol> <p>If no packages are configured, the base image (<code>asylum:latest</code>) is used directly.</p>"},{"location":"configuration/packages/#custom-build-commands","title":"Custom Build Commands","text":"<p>The <code>shell</code> kit's <code>build</code> key runs arbitrary commands during the project image build. Use this for tools that don't have a standard package manager:</p> <pre><code>kits:\n  shell:\n    build:\n      - \"curl -fsSL https://deno.land/install.sh | sh\"\n      - \"go install golang.org/x/tools/gopls@latest\"\n</code></pre> <p>Each command is executed as your host user. Commands must not contain newlines.</p>"},{"location":"development/building-from-source/","title":"Building from Source","text":""},{"location":"development/building-from-source/#prerequisites","title":"Prerequisites","text":"<ul> <li>Go (latest stable)</li> <li>Docker (for running, not building)</li> </ul>"},{"location":"development/building-from-source/#build","title":"Build","text":"<pre><code>git clone https://github.com/inventage-ai/asylum.git\ncd asylum\nmake build          # Build for current platform\nmake build-all      # Cross-compile all targets (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64)\n</code></pre> <p>The binary is output to <code>bin/asylum</code>.</p>"},{"location":"development/building-from-source/#test","title":"Test","text":"<pre><code>make test           # Unit tests\nmake test-integration  # Integration tests (requires Docker, slow)\n</code></pre> <p>Integration tests are gated behind <code>-tags integration</code> and excluded from <code>go test ./...</code>. They spin up real Docker containers and take several seconds per test.</p>"},{"location":"development/building-from-source/#dependencies","title":"Dependencies","text":"<p>Asylum has a single external dependency: <code>gopkg.in/yaml.v3</code>. Everything else is Go standard library.</p>"},{"location":"kits/","title":"Kits","text":"<p>Kits are modular bundles that group everything needed for a language or tool: installation, environment setup, caching, onboarding, and container configuration.</p>"},{"location":"kits/#available-kits","title":"Available Kits","text":"Kit Description Activation node Node.js LTS via fnm, global dev packages Always on python Python 3 with uv, linters, formatters Default java JDK 17/21/25 via mise, Maven, Gradle Default docker Docker-in-Docker with buildx and compose Default github GitHub CLI (gh) Default openspec OpenSpec CLI Default ssh SSH key management and mounting Always on shell oh-my-zsh, tmux, direnv Always on ports Automatic port forwarding for web services Always on ast-grep AST-based code search, lint, and rewrite (<code>sg</code>) Opt-in agent-browser Browser automation via agent-browser Opt-in cx Semantic code navigation for AI agents Opt-in apt Extra apt packages in the project image Opt-in"},{"location":"kits/#activation-tiers","title":"Activation Tiers","text":"<p>Kits have three activation levels:</p> Tier Behavior Always on Active even if not mentioned in config. Cannot be disabled. Default Added to your config automatically when first detected. Active when present in config. Opt-in Only active if you explicitly enable it in your config. <p>When Asylum detects new kits (e.g., after an update), it presents a single selection prompt with all new Default and Opt-in kits. Default kits are pre-selected; opt-in kits are unselected. Deselected kits are added as commented-out entries in your config.</p>"},{"location":"kits/#enabling-kits","title":"Enabling Kits","text":"<p>Add a kit to your <code>.asylum</code> config with an empty map to enable it with defaults, or with specific options:</p> <pre><code>kits:\n  docker: {}                  # enable with defaults\n  java:\n    versions: [\"17\", \"21\"]    # enable with options\n</code></pre>"},{"location":"kits/#disabling-kits","title":"Disabling Kits","text":"<p>Default-tier kits can be disabled:</p> <pre><code>kits:\n  github:\n    disabled: true\n</code></pre> <p>Always-on kits (node, ssh, shell, ports) cannot be disabled.</p>"},{"location":"kits/#kit-resolution","title":"Kit Resolution","text":"<p>When kits are configured explicitly (via config or <code>--kits</code> flag):</p> <ul> <li>Full kit (e.g., <code>java</code>) \u2014 activates the kit and all its sub-kits (maven, gradle)</li> <li>Specific sub-kit (e.g., <code>java/maven</code>) \u2014 activates the parent kit and only that sub-kit</li> <li>Always-on kits are added automatically regardless of config</li> </ul>"},{"location":"kits/#sub-kits","title":"Sub-Kits","text":"<p>Some kits contain sub-kits for optional features:</p> Kit Sub-Kits node <code>node/npm</code>, <code>node/pnpm</code>, <code>node/yarn</code> python <code>python/uv</code> java <code>java/maven</code>, <code>java/gradle</code> <p>When you enable a kit, all its sub-kits are included. To be selective, reference sub-kits directly with <code>--kits java/maven</code>.</p>"},{"location":"kits/#dependencies","title":"Dependencies","text":"<p>Kits can depend on other kits. For example, <code>openspec</code> depends on <code>node</code>. Missing dependencies are auto-activated at resolve time.</p>"},{"location":"kits/agent-browser/","title":"Agent-Browser Kit","text":"<p>Browser automation via agent-browser \u2014 an AI-native browser automation CLI.</p> <p>Activation: Opt-in \u2014 only active if explicitly enabled in your config.</p>"},{"location":"kits/agent-browser/#whats-included","title":"What's Included","text":"<ul> <li>agent-browser CLI for browser automation</li> <li>Chrome (Chrome for Testing, installed via <code>agent-browser install</code>)</li> <li>Claude Code skill \u2014 auto-mounted from the upstream project, teaching Claude the snapshot-ref interaction pattern</li> </ul>"},{"location":"kits/agent-browser/#configuration","title":"Configuration","text":"<pre><code>kits:\n  agent-browser: {}\n</code></pre> <p>The old <code>browser:</code> key still works as an alias.</p>"},{"location":"kits/agent-browser/#dependencies","title":"Dependencies","text":"<p>Depends on the Node.js kit (agent-browser is installed via npm).</p>"},{"location":"kits/agent-browser/#usage","title":"Usage","text":"<pre><code># Navigate to a page\nagent-browser open https://example.com\n\n# Get interactive elements with refs\nagent-browser snapshot -i\n\n# Interact using refs\nagent-browser click @e1\nagent-browser fill @e2 \"search query\"\n\n# Re-snapshot after page changes\nagent-browser snapshot -i\n</code></pre> <p>Run <code>agent-browser --help</code> for all commands.</p>"},{"location":"kits/apt/","title":"APT Packages Kit","text":"<p>Install extra system packages via apt in the project image.</p> <p>Activation: Opt-in \u2014 only active if explicitly enabled in your config.</p>"},{"location":"kits/apt/#whats-included","title":"What's Included","text":"<p>Nothing by default \u2014 this kit exists solely to install additional Debian packages you specify.</p>"},{"location":"kits/apt/#configuration","title":"Configuration","text":"<pre><code>kits:\n  apt:\n    packages:\n      - libpq-dev\n      - redis-tools\n      - ffmpeg\n</code></pre> Option Type Default Description <code>packages</code> list <code>[]</code> Debian packages to install via <code>apt-get install</code>"},{"location":"kits/apt/#how-it-works","title":"How It Works","text":"<p>Packages are installed as root during the project image build using <code>apt-get install -y --no-install-recommends</code>. The project image is cached and only rebuilt when the package list changes.</p>"},{"location":"kits/apt/#when-to-use","title":"When to Use","text":"<p>Use this kit for system libraries and tools that aren't covered by other kits. Common examples:</p> <ul> <li>Database client libraries: <code>libpq-dev</code>, <code>libmysqlclient-dev</code></li> <li>Media processing: <code>ffmpeg</code>, <code>imagemagick</code></li> <li>Additional CLI tools: <code>redis-tools</code>, <code>postgresql-client</code></li> </ul>"},{"location":"kits/ast-grep/","title":"ast-grep Kit","text":"<p>AST-based code search, lint, and rewrite via ast-grep (<code>sg</code>).</p> <p>Activation: Opt-in \u2014 only active if explicitly enabled in your config.</p>"},{"location":"kits/ast-grep/#whats-included","title":"What's Included","text":"<ul> <li>sg \u2014 CLI for searching, linting, and rewriting code using abstract syntax tree patterns</li> <li>Claude Code skill \u2014 auto-mounted from the upstream agent-skill project, teaching Claude how to write ast-grep rules</li> </ul>"},{"location":"kits/ast-grep/#configuration","title":"Configuration","text":"<pre><code>kits:\n  ast-grep: {}\n</code></pre>"},{"location":"kits/ast-grep/#dependencies","title":"Dependencies","text":"<p>Depends on the Node.js kit (installed via npm).</p>"},{"location":"kits/ast-grep/#usage","title":"Usage","text":"<pre><code># Search for a pattern\nsg run --pattern 'console.log($ARG)' --lang js\n\n# Lint with rules\nsg scan\n\n# Rewrite matches\nsg rewrite --pattern 'console.log($ARG)' --rewrite 'logger.info($ARG)' --lang js\n</code></pre> <p>Patterns use <code>$VAR</code> as wildcards to match any AST node. See the ast-grep documentation for full pattern syntax.</p>"},{"location":"kits/cx/","title":"cx Kit","text":"<p>Semantic code navigation for AI agents via cx.</p> <p>Activation: Opt-in \u2014 only active if explicitly enabled in your config.</p>"},{"location":"kits/cx/#whats-included","title":"What's Included","text":"<ul> <li>cx \u2014 CLI for semantic code navigation using tree-sitter grammars</li> </ul>"},{"location":"kits/cx/#configuration","title":"Configuration","text":"<pre><code>kits:\n  cx:\n    packages:            # tree-sitter language grammars to install\n      - python\n      - typescript\n      - go\n</code></pre> Option Type Default Description <code>packages</code> list <code>[]</code> Tree-sitter language grammars to install via <code>cx lang add</code>"},{"location":"kits/cx/#usage","title":"Usage","text":"<pre><code># File overview (table of contents with symbols)\ncx overview src/main.go\n\n# Search for symbols across the project\ncx symbols --query \"parse\"\n\n# Jump to a function definition\ncx definition --name parseArgs\n\n# Find all references to a symbol\ncx references --name Config\n\n# Add support for a language\ncx lang add python\n</code></pre> <p>cx uses tree-sitter for parsing, so language grammars must be installed for each language you want to navigate. Configure them in the <code>packages</code> list to have them installed automatically at image build time.</p>"},{"location":"kits/docker/","title":"Docker Kit","text":"<p>Full Docker-in-Docker support with buildx and compose.</p> <p>Activation: Default \u2014 added to config on first detection; active when present.</p>"},{"location":"kits/docker/#whats-included","title":"What's Included","text":"<ul> <li>Docker CE (full engine, not just CLI)</li> <li>docker-buildx-plugin</li> <li>docker-compose-plugin</li> </ul>"},{"location":"kits/docker/#configuration","title":"Configuration","text":"<pre><code>kits:\n  docker: {}\n</code></pre> <p>The Docker kit has no additional configuration options. Enable it with an empty map.</p>"},{"location":"kits/docker/#how-it-works","title":"How It Works","text":"<p>When the Docker kit is active:</p> <ol> <li>The container runs in privileged mode (required for Docker-in-Docker)</li> <li>The Docker daemon (<code>dockerd</code>) starts automatically inside the container</li> <li>The container user is added to the <code>docker</code> group</li> </ol> <p>The daemon uses the <code>vfs</code> storage driver and waits up to 30 seconds to be ready before the agent or shell starts.</p> <p>Note</p> <p>Privileged mode gives the container full access to the host kernel. This is required for Docker-in-Docker but reduces isolation. Only enable this kit if you need to build or run Docker containers inside Asylum.</p>"},{"location":"kits/github/","title":"GitHub CLI Kit","text":"<p>GitHub CLI (<code>gh</code>) for interacting with GitHub from within containers.</p> <p>Activation: Default \u2014 added to config on first detection; active when present.</p>"},{"location":"kits/github/#whats-included","title":"What's Included","text":"<ul> <li>gh \u2014 GitHub's official CLI for repos, PRs, issues, actions, and more</li> </ul>"},{"location":"kits/github/#configuration","title":"Configuration","text":"<pre><code>kits:\n  github:\n    credentials: auto   # share gh authentication from host\n    disabled: true       # disable this default-on kit\n</code></pre>"},{"location":"kits/github/#authentication","title":"Authentication","text":"<p>Enable <code>credentials: auto</code> to extract your <code>gh</code> auth token from the host (including system keyrings) and generate a <code>hosts.yml</code> inside the container. This lets <code>gh</code> authenticate without requiring <code>gh auth login</code> in every new container.</p> <p>Without credentials enabled, <code>gh</code> auth is part of your agent config, which is seeded from your host on first run and persisted in <code>~/.asylum/agents/&lt;agent&gt;/</code>.</p> <p>To authenticate manually inside a container:</p> <pre><code>asylum shell\ngh auth login\n</code></pre>"},{"location":"kits/java/","title":"Java Kit","text":"<p>JDK 17, 21, and 25 via mise, with Maven and Gradle available as sub-kits.</p> <p>Activation: Default \u2014 added to config on first detection; active when present.</p>"},{"location":"kits/java/#whats-included","title":"What's Included","text":"<ul> <li>JDK 17, 21, 25 installed via mise (default: 21)</li> <li>Maven (via sub-kit) with dependency caching</li> <li>Gradle (via sub-kit) with dependency caching</li> </ul>"},{"location":"kits/java/#configuration","title":"Configuration","text":"<pre><code>kits:\n  java:\n    default-version: \"17\"     # which JDK is the global default\n</code></pre> Option Type Default Description <code>default-version</code> string <code>\"21\"</code> JDK version to set as the global default <p>JDK 17, 21, and 25 are always pre-installed. You can install additional versions at runtime with <code>mise install java@&lt;version&gt;</code>.</p>"},{"location":"kits/java/#sub-kits","title":"Sub-Kits","text":""},{"location":"kits/java/#javamaven","title":"java/maven","text":"<p>Installs Maven via apt. Dependency cache persisted at <code>~/.m2</code>.</p>"},{"location":"kits/java/#javagradle","title":"java/gradle","text":"<p>Installs Gradle via mise. Dependency cache persisted at <code>~/.gradle</code>.</p>"},{"location":"kits/java/#version-switching","title":"Version Switching","text":"<p>Switch JDK version inside the container:</p> <pre><code>mise use java@17\nmise use java@25\n</code></pre> <p>You can also set the version via environment variable in your config:</p> <pre><code>kits:\n  java:\n    default-version: \"17\"\n</code></pre> <p>Or detect it automatically from <code>.tool-versions</code> in your project root.</p>"},{"location":"kits/java/#auto-detection","title":"Auto-Detection","text":"<p>If your project has a <code>.tool-versions</code> file with a <code>java</code> entry, Asylum reads the version from it and sets it as the default in the container.</p>"},{"location":"kits/node/","title":"Node.js Kit","text":"<p>Node.js LTS via fnm with global development packages.</p> <p>Activation: Always on \u2014 active in every container.</p>"},{"location":"kits/node/#whats-included","title":"What's Included","text":"<ul> <li>Node.js LTS managed by fnm</li> <li>Global packages: typescript, @types/node, ts-node, eslint, prettier, nodemon</li> <li>Package managers (via sub-kits): npm (built-in), pnpm, yarn</li> </ul>"},{"location":"kits/node/#configuration","title":"Configuration","text":"<pre><code>kits:\n  node:\n    packages:                    # additional global npm packages\n      - tsx\n      - vitest\n    shadow-node-modules: true    # default: true\n    onboarding: true             # auto-detect and install deps\n</code></pre> Option Type Default Description <code>packages</code> list <code>[]</code> Additional npm packages to install globally in the project image <code>shadow-node-modules</code> bool <code>true</code> Shadow <code>node_modules</code> with Docker volumes (see below) <code>onboarding</code> bool <code>false</code> Auto-detect lockfiles and run <code>npm install</code> on first container start"},{"location":"kits/node/#sub-kits","title":"Sub-Kits","text":""},{"location":"kits/node/#nodenpm","title":"node/npm","text":"<p>Included by default. Provides npm cache persistence at <code>~/.npm</code> and the npm onboarding task.</p>"},{"location":"kits/node/#nodepnpm","title":"node/pnpm","text":"<p>Installs pnpm globally.</p>"},{"location":"kits/node/#nodeyarn","title":"node/yarn","text":"<p>Installs yarn globally.</p>"},{"location":"kits/node/#shadow-node_modules","title":"Shadow node_modules","text":"<p>On macOS, Node.js native binaries built on the host won't work inside the Linux container. Asylum automatically shadows each <code>node_modules</code> directory with a named Docker volume so each platform has its own binaries. Your source files are shared \u2014 only <code>node_modules</code> is isolated.</p> <p>Disable with:</p> <pre><code>kits:\n  node:\n    shadow-node-modules: false\n</code></pre>"},{"location":"kits/node/#onboarding","title":"Onboarding","text":"<p>When <code>onboarding: true</code> is set, Asylum scans for lockfiles on first container start and runs the appropriate install command:</p> Lockfile Command <code>package-lock.json</code> <code>npm ci</code> <code>pnpm-lock.yaml</code> <code>pnpm install --frozen-lockfile</code> <code>yarn.lock</code> <code>yarn install --frozen-lockfile</code> <code>bun.lock</code> / <code>bun.lockb</code> <code>bun install --frozen-lockfile</code> <p>Onboarding state is tracked \u2014 it won't re-prompt unless a lockfile changes. Skip for a single run with <code>--skip-onboarding</code>.</p>"},{"location":"kits/node/#version-switching","title":"Version Switching","text":"<p>fnm is available inside the container. To switch Node versions:</p> <pre><code>fnm install 20\nfnm use 20\n</code></pre>"},{"location":"kits/openspec/","title":"OpenSpec Kit","text":"<p>OpenSpec CLI for structured change management.</p> <p>Activation: Default \u2014 added to config on first detection; active when present. Depends on the Node.js kit.</p>"},{"location":"kits/openspec/#whats-included","title":"What's Included","text":"<ul> <li>openspec \u2014 CLI for managing specs, changes, and proposals</li> </ul>"},{"location":"kits/openspec/#configuration","title":"Configuration","text":"<pre><code>kits:\n  openspec:\n    disabled: true    # disable this default-on kit\n</code></pre>"},{"location":"kits/openspec/#dependencies","title":"Dependencies","text":"<p>The OpenSpec kit depends on <code>node</code> because it's installed via npm. If the Node.js kit is not active, Asylum emits a warning.</p>"},{"location":"kits/openspec/#usage","title":"Usage","text":"<p>OpenSpec is used inside containers for structured change management:</p> <pre><code>openspec new change \"add-feature\"\nopenspec status --change \"add-feature\"\nopenspec list\n</code></pre> <p>See the OpenSpec documentation for full usage details.</p>"},{"location":"kits/openspec/#notes","title":"Notes","text":"<ul> <li>OpenSpec telemetry is disabled inside containers (<code>OPENSPEC_TELEMETRY=0</code>).</li> </ul>"},{"location":"kits/ports/","title":"Ports Kit","text":"<p>Automatic port forwarding for web services.</p> <p>Activation: Always on \u2014 active in every container.</p>"},{"location":"kits/ports/#what-it-does","title":"What It Does","text":"<p>The ports kit automatically allocates and forwards a range of high ports for each project. This means web servers, dev tools, and other services started inside the container are accessible from your host without manually specifying <code>-p</code> flags.</p>"},{"location":"kits/ports/#configuration","title":"Configuration","text":"<pre><code>kits:\n  ports:\n    count: 5          # number of ports to allocate (default: 5)\n</code></pre> Option Type Default Description <code>count</code> int <code>5</code> Number of ports to allocate per project"},{"location":"kits/ports/#how-it-works","title":"How It Works","text":"<p>Each project gets a unique range of consecutive ports starting from port 10000. The allocation is tracked globally in <code>~/.asylum/state.json</code> to prevent collisions between projects.</p> <p>For example, if your project gets ports 10000-10004:</p> <ul> <li>Start a server on port 10000 inside the container</li> <li>Access it at <code>localhost:10000</code> on your host</li> </ul> <p>The allocation is deterministic \u2014 the same project always gets the same port range (unless it's been freed and reallocated).</p>"},{"location":"kits/ports/#additional-ports","title":"Additional Ports","text":"<p>The ports kit handles automatic allocation. You can still forward specific ports manually with <code>-p</code>:</p> <pre><code>asylum -p 3000 -p 8080:80\n</code></pre> <p>Manual port forwards are in addition to the automatically allocated range.</p>"},{"location":"kits/python/","title":"Python Kit","text":"<p>Python 3 with uv for fast package management, plus pre-installed development tools.</p> <p>Activation: Default \u2014 added to config on first detection; active when present.</p>"},{"location":"kits/python/#whats-included","title":"What's Included","text":"<ul> <li>Python 3 (system package)</li> <li>uv \u2014 fast Python package installer and resolver</li> <li>Pre-installed tools: black, ruff, mypy, pytest, ipython, poetry, pipenv</li> <li>Build dependencies: python3-dev, python3-pip, python3-venv, libssl-dev, libffi-dev</li> </ul>"},{"location":"kits/python/#configuration","title":"Configuration","text":"<pre><code>kits:\n  python:\n    packages:          # additional tools to install via uv\n      - pandas\n      - numpy\n</code></pre> Option Type Default Description <code>packages</code> list <code>[]</code> Additional Python packages to install in the project image via <code>uv tool install</code>"},{"location":"kits/python/#sub-kits","title":"Sub-Kits","text":""},{"location":"kits/python/#pythonuv","title":"python/uv","text":"<p>Included by default. Provides:</p> <ul> <li>Pip cache persisted at <code>~/.cache/pip</code></li> <li>Auto-venv: automatically creates a <code>.venv/</code> virtual environment on container start if the project has <code>requirements.txt</code>, <code>pyproject.toml</code>, or <code>setup.py</code></li> </ul>"},{"location":"kits/python/#package-installation","title":"Package Installation","text":"<p>Packages listed in the config are installed with <code>uv tool install</code> during the project image build. For runtime dependencies in your project, use <code>uv pip install</code> or <code>pip install</code> inside the container.</p>"},{"location":"kits/rtk/","title":"rtk Kit","text":"<p>Token-reduction proxy via RTK \u2014 intercepts shell commands and compresses output to reduce LLM token usage by 60-90%.</p> <p>Activation: Opt-in \u2014 only active if explicitly enabled in your config.</p>"},{"location":"kits/rtk/#whats-included","title":"What's Included","text":"<ul> <li>rtk \u2014 CLI proxy that rewrites command outputs to strip noise (comments, whitespace, boilerplate)</li> <li>Claude Code hooks \u2014 auto-mounted PreToolUse hook that transparently intercepts Bash commands</li> </ul>"},{"location":"kits/rtk/#configuration","title":"Configuration","text":"<pre><code>kits:\n  rtk: {}\n</code></pre>"},{"location":"kits/rtk/#how-it-works","title":"How It Works","text":"<p>RTK registers a PreToolUse hook that intercepts every Bash command the agent runs. Commands like <code>git status</code>, <code>ls</code>, <code>grep</code>, etc. are transparently rewritten through RTK, which strips noise and compresses the output before the agent sees it.</p> <p>The hook and awareness doc are generated at image build time and mounted into <code>~/.claude/</code> at container start.</p>"},{"location":"kits/rtk/#usage","title":"Usage","text":"<pre><code># Check token savings\nrtk gain\n\n# See savings over time\nrtk gain --graph\n\n# Discover optimization opportunities\nrtk discover\n</code></pre> <p>RTK supports 100+ commands across files, git, testing, build/lint, and containers. See the RTK documentation for the full list.</p>"},{"location":"kits/shell/","title":"Shell Kit","text":"<p>Shell configuration: oh-my-zsh, tmux, and direnv.</p> <p>Activation: Always on \u2014 active in every container.</p>"},{"location":"kits/shell/#whats-included","title":"What's Included","text":"<ul> <li>oh-my-zsh with the <code>robbyrussell</code> theme</li> <li>tmux with a pre-configured setup</li> <li>direnv hooks in both bash and zsh</li> <li>PATH configuration for fnm and mise in both shells</li> </ul>"},{"location":"kits/shell/#configuration","title":"Configuration","text":"<pre><code>kits:\n  shell:\n    disabled: true           # disable this default-on kit\n    build:                   # custom commands to run at image build time\n      - \"curl -fsSL https://deno.land/install.sh | sh\"\n</code></pre> Option Type Default Description <code>disabled</code> bool <code>false</code> Disable this kit <code>build</code> list <code>[]</code> Custom shell commands to run during project image build"},{"location":"kits/shell/#tmux-configuration","title":"tmux Configuration","text":"<p>The pre-installed tmux config includes:</p> <ul> <li>Mouse support enabled</li> <li>256-color terminal</li> <li>50,000 line scroll history</li> <li><code>|</code> to split horizontally, <code>-</code> to split vertically</li> <li>Status bar: hostname (left), date/time (right)</li> </ul>"},{"location":"kits/shell/#custom-build-commands","title":"Custom Build Commands","text":"<p>The <code>build</code> option runs arbitrary commands during the project image build. This is the escape hatch for installing tools that don't fit into other kit categories:</p> <pre><code>kits:\n  shell:\n    build:\n      - \"curl -fsSL https://deno.land/install.sh | sh\"\n      - \"go install golang.org/x/tools/gopls@latest\"\n</code></pre> <p>Commands run as your host user. See Packages for more details.</p>"},{"location":"kits/ssh/","title":"SSH Kit","text":"<p>SSH key management for containers. Generates keys automatically and mounts them into <code>~/.ssh/</code>.</p> <p>Activation: Always On \u2014 active in every container, no configuration needed.</p>"},{"location":"kits/ssh/#what-it-does","title":"What It Does","text":"<p>On first container start, the SSH kit:</p> <ol> <li>Generates an Ed25519 key pair (if one doesn't exist)</li> <li>Prints the public key for you to add to GitHub/GitLab</li> <li>Mounts the key pair into <code>~/.ssh/</code> (read-only)</li> <li>Mounts the host's <code>~/.ssh/known_hosts</code> (read-write, if it exists)</li> </ol>"},{"location":"kits/ssh/#configuration","title":"Configuration","text":"<pre><code>kits:\n  ssh:\n    isolation: isolated   # default\n</code></pre>"},{"location":"kits/ssh/#isolation-modes","title":"Isolation Modes","text":"Mode Key Storage Behavior <code>isolated</code> (default) <code>~/.asylum/ssh/</code> Shared across all projects, separate from host <code>shared</code> Host <code>~/.ssh/</code> Mounts entire host SSH directory (read-write) <code>project</code> <code>~/.asylum/projects/&lt;container&gt;/ssh/</code> Separate key pair per project"},{"location":"kits/ssh/#isolated-default","title":"Isolated (default)","text":"<p>Keys are generated in <code>~/.asylum/ssh/</code> and shared across all projects. The host's <code>~/.ssh/known_hosts</code> is mounted read-write so new host keys added inside containers are available on the host.</p>"},{"location":"kits/ssh/#shared","title":"Shared","text":"<p>The host's entire <code>~/.ssh/</code> directory is mounted read-write. No key generation occurs. Use this if you want to use your existing host SSH keys directly.</p>"},{"location":"kits/ssh/#project","title":"Project","text":"<p>Each project gets its own key pair in <code>~/.asylum/projects/&lt;container&gt;/ssh/</code>. Useful if you need different deploy keys per project. The host's <code>~/.ssh/known_hosts</code> is still mounted read-write.</p>"},{"location":"kits/ssh/#replacing-keys","title":"Replacing Keys","text":"<p>You can replace the generated key with your own by placing your key files in the appropriate directory:</p> <ul> <li>Isolated: <code>~/.asylum/ssh/</code></li> <li>Project: <code>~/.asylum/projects/&lt;container&gt;/ssh/</code></li> </ul>"}]}