# Spec: copilot-mcp

## Goal

- Define minimal environment/flags to expose an MCP server to Copilot.

## Requirements

- Asylum SHALL provide a configurable mount path (e.g., /opt/mcp) and an env var (COPILOT_MCP_DIR) if Copilot consumes it.

Notes: Implementation deferred after spec refinement
