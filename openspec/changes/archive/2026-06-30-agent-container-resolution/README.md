# agent-container-resolution

Resolve a per-project container by agent set: reuse when the requested agent is supported, otherwise spill to a hash(project+agents) container instead of rebuilding in place
